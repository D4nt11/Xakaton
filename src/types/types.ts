export interface IUser{
    id: string
    email: string
    firstName: string
    lastName: string
    patronymic?: string
    idCard: string
    dateOfBirth: string
}

export interface IDoctor{
    id: string
    email: string
    firstName: string
    lastName: string
    patronymic?: string
    dateOfBirth: string
}